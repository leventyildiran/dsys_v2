import 'package:flutter/material.dart';

class FaturaScreen extends StatelessWidget {
  const FaturaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Fatura Modülü (Yapım Aşamasında)'),
    );
  }
}
