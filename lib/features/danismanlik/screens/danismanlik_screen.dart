import 'package:flutter/material.dart';

class DanismanlikScreen extends StatelessWidget {
  const DanismanlikScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Danışmanlık Takibi (Yapım Aşamasında)'),
    );
  }
}
